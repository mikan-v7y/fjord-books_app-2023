<h1><%= t('.title') %></h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, user_path(user) %>
    </li>
  <% end %>

  <br>

  <%= paginate @users %>

  <br>

  <%= link_to t('.back'), books_path %>
</ul>
